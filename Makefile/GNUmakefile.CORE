###################################################

# default target list
ROOTMAP=$(SHLIB:.so=.rootmap)
FOOLIGHT_BASIC_ROOTLIBS = $(shell root-config --libs)
FOOLIGHT_BASIC_ROOTINC  = $(shell root-config --cflags)

all: pre_build main_build 

pre_build:

main_build: pre_build .depend $(DICT).cxx $(FOOLIGHT_LIBDIR)/$(SHLIB) $(FOOLIGHT_LIBDIR)/$(ROOTMAP) 

# default rule for making .o files from .cxx files
%.o: %.cxx
	@echo '<< compiling' $< '>>'
	@$(CXX) $(CXXFLAGS) $(FOOLIGHT_BASIC_ROOTINC) $(INCFLAGS) -c $<

ifdef FOOLIGHT_ROOT6

# root class dictionary
$(DICT).cxx: $(HEADERS) LinkDef.h
	@echo '<< generating dict >>'
	@rootcling -f $(DICT).cxx -s $(FOOLIGHT_LIBDIR)/$(SHLIB) -rml $(SHLIB) -rmf $(FOOLIGHT_LIBDIR)/$(ROOTMAP) -c $(CINTFLAGS) $(INCFLAGS) $(HEADERS) LinkDef.h

# rule for making shareable library
$(FOOLIGHT_LIBDIR)/$(SHLIB): $(OBJECTS) $(DICT).cxx $(DICT).o
	@echo '<< generating $(SHLIB) >>'
	@$(SOMAKER) -o $(FOOLIGHT_LIBDIR)/$(SHLIB) $(filter-out $(DICT).o, $(OBJECTS)) $(DICT).o $(SOFLAGS) $(LDFLAGS) $(FOOLIGHT_BASIC_ROOTLIBS) 

# if ROOTMAP defined, define rule for making it
$(FOOLIGHT_LIBDIR)/$(ROOTMAP): $(FOOLIGHT_LIBDIR)/$(SHLIB)

else

# root class dictionary
$(DICT).cxx: $(HEADERS) LinkDef.h
	@echo '<< generating dict >>'
	@rootcint -f $(DICT).cxx -c $(CINTFLAGS) $(INCFLAGS) $(HEADERS) LinkDef.h

# rule for making shareable library
$(FOOLIGHT_LIBDIR)/$(SHLIB): $(OBJECTS) $(DICT).cxx $(DICT).o
	@echo '<< generating $(SHLIB) >>'
	@mkdir -p lib
	@$(SOMAKER) $(SOFLAGS) -o $(FOOLIGHT_LIBDIR)/$(SHLIB) $(filter-out $(DICT).o, $(OBJECTS)) $(DICT).o $(LDFLAGS) $(FOOLIGHT_BASIC_ROOTLIBS) 
	@echo '<< installing $(SHLIB) >>'

# if ROOTMAP defined, define rule for making it
$(FOOLIGHT_LIBDIR)/$(ROOTMAP): $(FOOLIGHT_LIBDIR)/$(SHLIB)
	@echo "<< generating rootmap >>"
	@rlibmap -f  -o $(FOOLIGHT_LIBDIR)/$(ROOTMAP) -l $(FOOLIGHT_LIBDIR)/$(SHLIB) -c LinkDef.h

endif
###################################################

# "make clean" target
clean: pre_clean
	@echo '<< cleaning all >>'
	@rm -f .depend
	@rm -f $(DICT).* *.o 
	@rm -f lib/*.o lib/lib*.so lib/lib*.rootmap lib/lib*.pcm
	@rm -f $(FOOLIGHT_LIBDIR)/$(SHLIB) $(FOOLIGHT_LIBDIR)/$(SHLIB:.so=.rootmap) $(FOOLIGHT_LIBDIR)/$(SHLIB:.so=_rdict.pcm)

pre_clean: 

###################################################
libclean:
	@echo '<< cleaning lib directory >>'
	@rm -f $(FOOLIGHT_LIBDIR)/$(SHLIB) 
	@rm -f lib/$(SHLIB) 

###################################################
clearCINT:
	@echo '<< removing CINT >>'
	@rm -f $(DICT).*


###################################################
# auto-generate dependencies using g++ -M
.depend: $(SOURCES) $(HEADERS)
	@#rm .depend
	@echo '<< checking dependencies >>'
	@$(CXX) -M $(CXXFLAGS) $(FOOLIGHT_BASIC_ROOTINC) $(INCFLAGS) $(SOURCES) > .depend

-include .depend

#ALWAYS:
